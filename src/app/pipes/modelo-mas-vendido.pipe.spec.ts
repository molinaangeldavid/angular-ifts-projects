import { ModeloMasVendidoPipe } from './modelo-mas-vendido.pipe';

describe('ModeloMasVendidoPipe', () => {
  it('create an instance', () => {
    const pipe = new ModeloMasVendidoPipe();
    expect(pipe).toBeTruthy();
  });
});
